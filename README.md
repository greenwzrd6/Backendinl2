#Backendinl2

to use run.sh, run `chmod +x run.sh` and then `./run.sh`

if you run into an error you might need to delete the database files that are created, but that should not be needed since I am dropping the calls and customers table in the CustomerDaoJdbcTemplateImpl constructor
